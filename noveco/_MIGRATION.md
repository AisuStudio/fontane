# Noveco — hier nur zwischengelagert (nicht Teil von fontane)

Dieser Ordner ist das eigenständige Projekt **Noveco**. Er liegt hier nur zum Transport,
weil ein direkter Push nach `AisuStudio/Noveco` aus der Build-Session blockiert war
(403 / nicht autorisiert).

## Migration am Rechner
1. Inhalt von `noveco/` (ohne diese Notiz) ins Repo `AisuStudio/Noveco` kopieren.
2. `git add -A && git commit -m "Noveco MVP: mobile-first app, waffle token layer, Maßnahmen-Tab" && git push`
3. Ordner `noveco/` danach aus fontane entfernen.

Kontext & nächste Schritte: siehe `noveco/BRIEFING.md`.
